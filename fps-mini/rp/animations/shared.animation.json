{
	"format_version": "1.8.0",
	"animations": {
		"animation.template.weapons.fp.rot": {
			"loop": "hold_on_last_frame",
			"bones": {
				"root": {
					"rotation": [0, "-q.mod.is_first_person ? -180 : -0", 0]
				},
				"body": {
					"rotation": ["q.mod.x_rot", "q.mod.y_rot", 0],
					"position": [
						"v.fpbody_px = math.lerp(v.fpbody_px ?? 0, -math.cos(q.modified_distance_moved * (q.is_sprinting ? 25 : 20)) * math.clamp(q.modified_move_speed, 0, 1) * (q.is_sprinting ? 0.4 : 0.2), 0.1); return v.fpbody_px;",
						"v.fpbody_py = math.lerp(v.fpbody_py ?? 0, math.sin(q.modified_distance_moved * (q.is_sprinting ? 50 : 40)) * math.clamp(q.modified_move_speed, 0, 1) * (q.is_sprinting ? 0.5 : 0.15), 0.1); return v.fpbody_py;",
						"v.fpbody_pz = math.lerp(v.fpbody_pz ?? 0, math.cos(q.modified_distance_moved * (q.is_sprinting ? 25 : 20)) * math.clamp(q.modified_move_speed, 0, 1) * (q.is_sprinting ? 0.3 : 0.1), 0.1); return v.fpbody_pz;"
					]
				}
			}
		}
	}
}