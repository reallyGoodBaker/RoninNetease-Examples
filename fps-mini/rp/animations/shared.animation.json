{
	"format_version": "1.8.0",
	"animations": {
		"animation.template.weapons.fp.rot": {
			"loop": "hold_on_last_frame",
			"bones": {
                "fpsroot": {
					"rotation": [0, "q.mod.is_first_person ? 180 : 0", 0],
                    "position": [0, "v.smoothvspeed = math.lerp(v.smoothvspeed ?? 0, math.clamp(q.vertical_speed, -3, 3), 0.1); v.smoothvspeed2 = math.lerp(v.smoothvspeed2 ?? 0, math.clamp(v.smoothvspeed, -3, 3), 0.05); v.smoothvspeed3 = math.lerp(v.smoothvspeed3 ?? 0, math.clamp(v.smoothvspeed2, -3, 3), 0.05); return v.smoothvspeed3 * -0.3 * q.mod.shake_scale;", 0]
				},
                "control": {
                    "rotation": [
                        "q.mod.x_rot",
                        "q.mod.y_rot",
                        0
                    ],
                    "position": [
                        "v.fpbody_px = math.lerp(v.fpbody_px ?? 0, -math.abs(math.cos(q.modified_distance_moved * q.mod.walk_scale * (q.is_sprinting ? 14 : 12))) * math.clamp(q.modified_move_speed, 0, 1) * (q.is_sprinting ? 0.8 : 0.4), 0.1) * q.mod.walk_scale; return v.fpbody_px + q.mod.control_pos_x;",
						"v.fpbody_py = math.lerp(v.fpbody_py ?? 0, -math.abs(math.sin(q.modified_distance_moved * q.mod.walk_scale * (q.is_sprinting ? 28 : 24))) * math.clamp(q.modified_move_speed, 0, 1) * (q.is_sprinting ? 1.2 : 0.8), 0.1) * q.mod.walk_scale; v.breath_y = math.lerp(v.breath_y ?? 0, math.abs(math.sin(q.life_time * 30)), 0.5) * 0.3 * q.mod.shake_scale; return v.fpbody_py + q.mod.control_pos_y + v.breath_y;",
						"v.fpbody_pz = math.lerp(v.fpbody_pz ?? 0,  math.abs(math.cos(q.modified_distance_moved * q.mod.walk_scale * (q.is_sprinting ? 14 : 12))) * math.clamp(q.modified_move_speed, 0, 1) * (q.is_sprinting ? 0.8 : 0.5), 0.1) * q.mod.walk_scale; return v.fpbody_pz + q.mod.control_pos_z;"
                    ],
                    "scale": [ 1, 1, "q.mod.model_scale" ]
                },
                "crosshair": {
                    "rotation": [
                        "q.mod.c_rot_x",
                        "q.mod.c_rot_y",
                        "q.mod.c_rot_z"
                    ]
                }
			}
		}
	}
} 